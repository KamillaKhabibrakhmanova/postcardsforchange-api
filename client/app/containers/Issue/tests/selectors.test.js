// import { fromJS } from 'immutable';
// import { makeSelectIssueDomain } from '../selectors';

// const selector = makeSelectIssueDomain();

describe('makeSelectIssueDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
