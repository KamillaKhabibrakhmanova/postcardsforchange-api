// import { fromJS } from 'immutable';
// import { makeSelectSplashDomain } from '../selectors';

// const selector = makeSelectSplashDomain();

describe('makeSelectSplashDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
